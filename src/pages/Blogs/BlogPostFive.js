import React from 'react';

const BlogPostFive = () => {
    return (
        <div className='mt-10 max-w-lg'>
            <h1 className='text-xl text-primary font-bold'> You have an array of products. Each product has a name, price, description, etc. How will you implement a search to find products by name?</h1>

            <div className='mt-5'>
                <p>I  use the Array.find() method to implement a search to find products bye name</p>
            </div>

        </div>
    );
};

export default BlogPostFive;